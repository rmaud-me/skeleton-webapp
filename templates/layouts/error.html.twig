{% extends 'layouts/base.html.twig' %}

{% block importmap %}
    {{ importmap(['app', 'pages-error']) }}
{% endblock %}

{% block body %}
<div id="error">
    <div class="error-page container">
        <div class="col-md-8 col-12 offset-md-2">
            <div class="text-center">
                {% block content %}{% endblock %}
                <a href="{{ path('security_login') }}" class="btn btn-lg btn-outline-primary mt-3">Go Home</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
